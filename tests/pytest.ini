[pytest]
testpaths = unit integration e2e
markers =
    integration: requires live infrastructure (Neo4j, Live-Memory, etc.)
    e2e: end-to-end tests requiring full stack including NetLab
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
